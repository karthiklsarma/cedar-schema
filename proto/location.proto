syntax = "proto3";

package gen;

// protoc --go_out=. *.proto to generate proto files in the destination
option go_package = "../gen/";

message Location {
    string id = 1;
    double lat = 2;
    double lng = 3;
    int64 timestamp = 4;
    string device = 5;
}